// Derived from the real sidebar nav config (src/navigation/vertical/apps.js)
// so this grid can never list a module that isn't actually in the menu.
export { menuPermissionGroups, allMenuPermissionIds } from '../../../../navigation/permissionModules'
